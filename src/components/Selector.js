import React, { Component } from 'react';

class Selector extends Component {
    render(){
      return (
        <div>
          <a href="/main" onClick={function(e){

          }.bind(this)}>Selector1</a>
        </div>
      );
    }
  }

  export default Selector
